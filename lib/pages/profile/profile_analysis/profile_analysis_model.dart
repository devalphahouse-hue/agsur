/// Stub do model — a tela foi reescrita com state local e não precisa
/// mais do par FlutterFlow `*_widget` + `*_model`. Mantido apenas para
/// preservar o contrato de import/export existente em outras partes.
class ProfileAnalysisModel {
  void dispose() {}
}
